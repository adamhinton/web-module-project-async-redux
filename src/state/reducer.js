const initState = {
    loading: false,
    error: '',
    success: {},
}

const reducer = (state = initState, action) =>{
    switch(action.type){
        default:
            return state;
    }
}

export default reducer;